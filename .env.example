# Base URL of your Express/Node backend REST API
VITE_API_BASE_URL=http://localhost:5000/api

# "true" -> use in-memory mock data (no backend needed, works out of the box)
# "false" -> use apiClient to call VITE_API_BASE_URL (real MERN backend)
VITE_USE_MOCK_API=true
